# 《此生为蛇》当前状态

> 本文件只记录当前可验证状态，不复制历史流水。当前代码与 WIP 以实时 `git HEAD` 和 `git status` 为准；本文不把未提交工作树内容写成完成基线。

## 已完成或已进入基线

- 主链路：蛇移动、成长、敌潮、技能/Combo、HUD、Boss 与结算流程已存在于当前代码。
- 满级溢出：当前 HEAD 的 `09_wave.js` 负责满级溢出后的回血/食物处理，`16_skill_econ.js` 负责相关统计。
- Boss：当前 HEAD 已有 Phase 1 → Phase 2 状态切换与弹幕发射基线。
- 工程模型：原生 JS + Canvas + Web Audio；脚本顺序、全局模块、Bus 通信、CONFIG 入口和固定步进循环是当前架构契约。
- 音频：BGM、SFX、UI 反馈、生命周期和基础 voice/density 管理已形成可工作的基线；这不是最终 creative golden master，Skill/Combo 音色仍允许专项重设计。
- 移动端：横屏门控、HUD 安全区、摇杆和 iOS 音频解锁路径已有实现；完整 standalone 发布回归仍按触发条件执行。
- 表现：火焰、电系、Boss 和元素状态已有阶段性实现；这些是当前可用方案，不是未来所有技能的固定视觉模板。
- 边缘撞墙回正：已在当前 HEAD 的蛇移动实现中存在，仍需用户实机确认手感。

## 进行中 / 待用户验收

- B-TUNE：`13_editor.js` 与 `07_enemy.js` 已有 `DEBUG.enabled && DEBUG.editorEnabled` 门控代码；但当前 HEAD 的 CONFIG 默认标志仍为开启，不能宣称发布默认关闭已完成，需单独确认发布口径和三种组合。
- 火焰与 Boss 降噪、元素图标、移动端主流程和边缘回正需要用户实机验收；浏览器截图仅在用户明确要求时执行。
- 音频基础流程可继续使用，但技能/Combo 的创意定稿、手机外放和 standalone 主流程仍需按 QA 范围复核。

## 阻塞与开放问题

- CONFIG 深冻结、`RENDER.worldScale` 双源、RT 单位/索引和 AOE 判定口径仍需专项确认。
- Boss 弹幕是否成为正式伤害源等具体机制仍需对应专项决定；不从 GDD 推断实现状态。
- 移动端极端窄屏、完整 iOS standalone 发布前主流程和最终美术/音频 creative 方向仍未整体封版。
- 具体债务以当前代码核对结果为准；本状态页不把历史计划中的“待办”自动继承为当前问题。

## 近期工作入口

1. 先处理当前 worktree 中已经明确授权的 WIP，保持代码、资源和文档改动隔离。
2. 对发布/移动端/音频主流程做按需 QA，而不是重复历史清单。
3. 对开放的 CONFIG、碰撞、Boss 和窄屏问题分别建立专项，先确认事实和验收目标再改代码。
